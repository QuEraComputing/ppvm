[package]
name = "ppvm-tobc"
version = "0.1.0"
edition = "2024"

[dependencies]
ndarray = "0.17.2"
num = "0.4.3"
ppvm-runtime = { version = "0.1.0", path = "../ppvm-runtime" }
ppvm-sym = { version = "0.1.0", path = "../ppvm-sym" }
