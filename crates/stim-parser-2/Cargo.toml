[package]
name = "stim-parser-2"
version = "0.1.0"
edition = "2024"

[dependencies]
chumsky = "0.12.0"
thiserror = "2.0.18"

[dev-dependencies]
proptest = "1"
stim-parser = { version = "0.1.0", path = "../stim-parser" }   # parity harness only; removed at swap
